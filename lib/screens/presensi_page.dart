import 'package:flutter/material.dart';

class PresensiPage extends StatelessWidget {
  const PresensiPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Presensi Page", style: TextStyle(fontSize: 20)),
    );
  }
}
